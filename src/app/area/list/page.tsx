import { AreaCards } from "../components/area-cards";

export default function ListAreas() {
  return (
    <section className="flex min-h-screen flex-col items-center justify-center px-20">
      <AreaCards />
    </section>
  );
}
